wiremock:
    server:
        port: 0
botapi:
    webhook_base: "testurl"
    viber:
        bot_token: ${SCHEDULIFY_VIBER_BOT_TOKEN}

    telegrambot:
        server_url: http://localhost:${wiremock.server.port}
        name: ${SCHEDULIFY_TELEGRAM_BOT_NAME}
        token: ${SCHEDULIFY_TELEGRAM_BOT_TOKEN}